{0 ModelKit}

ModelKit is a portable OCaml library for cohesive classical machine learning
workflows.

The current engineering foundation provides opaque float64 {!Modelkit.Vector}
and {!Modelkit.Matrix} values, immutable {!Modelkit.Row_view} selections,
validated targets and aligned metadata. Public estimator, transformer, scorer,
splitter, execution, RNG, and numerical-backend module types define the
extension boundaries. Concrete implementations and end-to-end workflows will
arrive in later milestones.
