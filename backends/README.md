# Optional backends

This directory is reserved for optional numerical and execution backends.
Planned backends depend inward on `modelkit` and must conform to the portable
sequential implementation; the portable package never requires an accelerated
backend.

No backend package is implemented or published yet.
