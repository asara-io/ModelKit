# Optional adapters

This directory is reserved for optional packages that convert ecosystem data
into ModelKit's portable representations. Planned adapters depend inward on
`modelkit`; the portable package never depends on an adapter.

No adapter package is implemented or published yet.
